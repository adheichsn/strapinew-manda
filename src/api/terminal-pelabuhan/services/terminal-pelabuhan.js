'use strict';

/**
 * terminal-pelabuhan service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::terminal-pelabuhan.terminal-pelabuhan');
